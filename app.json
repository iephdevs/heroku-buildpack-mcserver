{
  "name": "Minecraft",
  "description": "A Minecraft Server on Heroku",
  "keywords": [
    "games",
    "minecraft"
  ],
  "repository": "https://github.com/vigrox/heroku-buildpack-mcserver",
  "env": {
    "MINECRAFT_EULA": {
      "description": "Do you accept the Minecraft EULA?",
      "value": "true"
    },
	"MINECRAFT_VERSION": {
      "description": "Where to download the minecraft jar?",
      "value": "https://cdn.getbukkit.org/spigot/spigot-1.8-R0.1-SNAPSHOT-latest.jar"
    },
    "NGROK_API_TOKEN": {
      "description": "Your personal ngrok API token",
      "required": true
    },
    "AWS_BUCKET": {
      "description": "S3 Bucket to store your server state (you will lose your games when the server restarts if you don't do this)",
      "required": false
    },
    "AWS_ACCESS_KEY": {
      "description": "The AWS access key for the S3 Bucket",
      "required": false
    },
    "AWS_SECRET_KEY": {
      "description": "The AWS secret key for the S3 Bucket",
      "required": false
    }
  },
  "buildpacks": [
    {
      "url": "heroku/jvm"
    },
    {
      "url": "heroku-community/inline"
    }
  ]
}